spring:
  datasource:
    url: jdbc:h2:mem:chatdb
    username: sa
    password:
  data:
    redis:
      host: localhost
      port: 63791
      database: 0

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

  application:
    name: chatserver
